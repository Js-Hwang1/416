# Spring Boot application configuration
#
# Server port (default 8080):
# server.port=8080
#
# MongoDB connection:
# spring.data.mongodb.uri=mongodb://localhost:27017/redistricting
# spring.data.mongodb.database=redistricting
#
# Logging level (useful for debugging):
# logging.level.org.springframework.data.mongodb=DEBUG
#
# Coordinate with Karen (database team) on the actual MongoDB URI and database name.
