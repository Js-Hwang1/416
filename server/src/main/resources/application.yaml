server:
  port: 8081
  compression:
    enabled: true
    mime-types: application/json,application/octet-stream,application/x-protobuf,application/vnd.mapbox-vector-tile
    min-response-size: 1024

spring:
  application:
    name: demo
  data:
    mongodb:
      uri: "mongodb://localhost:27017/tigers-db"
